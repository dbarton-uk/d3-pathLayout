var TestData = {
    buildNodes: function (n) {

        var nodes = [];

        for (var i = 0; i < n; i++)
            nodes.push({});

        return nodes;

    },

    path: [
        [20, 100], [200, 100]
    ]

};
exports.TestData = TestData;
